# Python packages this project needs.
# Install them all with:  pip install -r requirements.txt

# Anthropic's official Python SDK for the Claude API
anthropic>=0.39

# Loads your API key from a local .env file so it stays out of your code
python-dotenv>=1.0
